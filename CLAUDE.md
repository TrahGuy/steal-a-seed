# Steal a Seed — Claude entrypoint

Read [AGENTS.md](AGENTS.md) completely before doing any project work. It is the single canonical
guide shared by Claude, Codex, and later agents.

Also read [KB/HANDOFF.md](KB/HANDOFF.md) for current state and
[KB/BLUEPRINT.md](KB/BLUEPRINT.md) for design direction. Inspect `git status` and recent commits
before editing; the checkout is shared and may contain another agent's in-progress work.

Do not copy project rules back into this file. Update `AGENTS.md` once when shared guidance changes.
Canonical skills live in `.agents/skills/`; entries under `.claude/skills/` are compatibility
pointers to those same files.
